<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yun.opern.dao.CategoryInfoDao">

    <select id="getBaseCategoryInfo" resultType="com.yun.opern.model.category.BaseCategoryInfo">
        SELECT
            categoryOne,
            categoryTwo
        FROM tbl_operninfo
        WHERE categoryOne &lt;&gt; '' AND categoryTwo &lt;&gt; ''
        GROUP BY categoryOne, categoryTwo
        ORDER BY categoryOne, categoryTwo
    </select>

</mapper>